import { IProject } from '@/app/_server/lib/types/constants';
import { MongooseToFrontend } from '@/lib/types/general';

export type FullProject = MongooseToFrontend<IProject>;

export default function LegislativeWorkPage() {
  return <></>;
}
